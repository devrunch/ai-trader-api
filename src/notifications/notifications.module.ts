import { Module } from '@nestjs/common';

/**
 * Intentional placeholder — web-push notifications are planned, and the VAPID_*
 * keys in `.env` exist for it. It is empty because the work has not started,
 * not because it is broken.
 */
@Module({})
export class NotificationsModule {}
